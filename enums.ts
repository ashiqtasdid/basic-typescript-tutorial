// We can use enums to declare a set of named constants i.e. a collection of related values that can be numeric or string values.

const enum Role {
    Admin,
    Moderator,
    User,
    Subscriber
}

const myRole = Role.Admin
