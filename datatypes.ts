// In this tutorial, we will be using multiple types.

// We have to define the types first

// for string 

let Name: string = 'Ashiq';
// for number

let age: number = 18; // we can also use float
let currentAge: number = 18.5;

// for boolean

let isAlive: boolean = true;

// for array

let hobbies: string[] = ['Coding', 'Gaming', 'Streaming'];
let money: number[] = [20, 30, 2];

// for two types of data

let mixed: (string | number)[] = ['Ashiq', 18, 20, 'Mars'];